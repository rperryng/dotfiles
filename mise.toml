# Tasks live as standalone scripts in ./mise-tasks/ (auto-discovered by mise).
# See https://mise.jdx.dev/tasks/file-tasks.html
# This file anchors the project root (MISE_PROJECT_ROOT) for those tasks.
